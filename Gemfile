# Pins the same toolchain GitHub Pages uses, so a local preview matches
# production exactly. You don't need Ruby installed — see the Docker command
# in README.md ("Preview the blog locally").
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick" # required to run `jekyll serve` on Ruby 3+
